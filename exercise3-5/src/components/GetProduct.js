/*import React from 'react'
import styles from './GetProduct.module.css'

export default function GetProduct(props) {
    return (
        <div className={ styles.product }>
            <div><img  className={ styles.imge } src={`/images/${props.image}`} /></div>
            <div className={ styles.productInfoItem }>
                
                <div className={ styles.text }>
                        <div className={ styles.productName }>{ props.productName }</div>
                        <div>${ props.price }</div>
                        <div>{ props.pricePerCount }</div>
                        <div>Rating: { props.rating }</div>
                </div>
            </div>
        </div>
    )
}
*/